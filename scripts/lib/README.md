# Script libraries

Тут розміщуватимуться спільні shell-функції з чітким contract і тестами. Runtime environment files не можна завантажувати через `source` або `.`; значення мають оброблятися strict parser або передаватися інструменту напряму.
